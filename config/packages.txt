# claw-linux Alpine OS package list
# These packages are installed into the base OS image via `apk add`.
# Add or remove packages here; the Dockerfile reads this file at build time.

# Core system utilities
bash
busybox-extras
coreutils
util-linux
procps
shadow
sudo

# Networking
curl
wget
ca-certificates
bind-tools
iputils
openssh-client

# Runtime tools
git

# Python runtime (for the autonomous agent)
python3
py3-pip

# Text processing
jq
sed
grep

# Process management
htop

# ── claw-linux skill binary dependencies ─────────────────────────────────────
# Required by specific claw-linux skill binaries.
# espeak-ng: claw-tts (text-to-speech)
# mpv:       claw-media (media playback)
# ffmpeg:    claw-media (metadata via ffprobe)
# xdg-utils: claw-browser, claw-canvas (xdg-open on XFCE/Wayland)
# w3m:       claw-browser (headless fallback text browser)
espeak-ng
mpv
ffmpeg
xdg-utils
w3m

# ── Desktop (XFCE) ────────────────────────────────────────────────────────────
# Installed when CLAW_MODE=desktop or when building a bare-metal ISO.
# These are listed here for reference; the ISO build script installs them
# via apk in the live environment overlay.
# xfce4
# xfce4-terminal
# xfce4-panel
# xfce4-settings
# xfwm4
# xfdesktop
# xfce4-session
# lightdm
# lightdm-gtk-greeter
# dbus-x11
# xorg-server
# xf86-video-vesa
# xf86-input-libinput
# mesa-dri-gallium
# font-dejavu
# adwaita-icon-theme
# network-manager
# network-manager-applet
# mousepad
# ristretto
# thunar
